package cl.uchile.dcc
package gwent.controller

import scala.collection.mutable.ArrayBuffer
import gwent.cards.Cards

class GameController(private var deckOfCardsPlayer:ArrayBuffer[Cards],private var deckOfCardsCPU:ArrayBuffer[Cards]){
   private var gameState :State = new GameInitialization()








}
